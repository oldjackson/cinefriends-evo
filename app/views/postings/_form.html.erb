
<%#= simple_form_for @posting do |f| %>
    <%#= datepick_input f, :date, :end_date %>
    <!-- <div class="form-actions text-center"> -->
      <%#= f.submit "Post", class: 'btn btn-search btn-block' %>
    <!-- </div> -->
<%#end %>



<%= form_tag movie_postings_path(@movie), method: :post do %>

  <div class="form-group">
    <%= text_field_tag :date, params[:date], class: "form-control", placeholder: "Date...", id: "show_date" %>
  </div>
  <div class="form-group">
    <%= select_tag :theater, options_from_collection_for_select([], "id", "name"), {class: "form-control", id: "show_theater", disabled: true, include_blank: true} %>
  </div>
  <div class="form-group">
    <%= select_tag :time, options_from_collection_for_select([], "id", 'uf_time'), {class: "form-control", id: "show_time", disabled: true, include_blank: true} %>
  </div>
  <%= submit_tag 'Post', name: nil, class: "btn btn-search btn-block" %>

<% end %>
