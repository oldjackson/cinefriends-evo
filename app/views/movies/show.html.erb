<div class="container">
  <h1>CineFriends for <%= @movie.title %></h1>

  <div class="main-container full-padded">

    <div class="poster-container full-padded" >
      <div class="cardfilm-large">
        <% if @movie.poster.file.nil? %>
          <div class="empty-poster">
            <p>-- No picture --</p>
          </div>
        <% else %>
          <div class="poster">
            <%= cl_image_tag @movie.poster, height: 450, width: 300 %>
          </div>
        <% end %>

        <div class="movie-label">
          <div class="film-name-large">
            <h2><%= @movie.title %></h2>
            </div>
          <div class="film-director-large"><%= "#{@movie.director}" %></div>
        </div>
      </div>
    </div>

    <div id="postings-container">
        <div class="row">
          <% @postings.each do |posting| %>
            <div class="col-xs-12 col-sm-6 full-padded">
              <div class="card-wrapper">
                <div class="carduser">
                  <div class="cardimageuser", style="background-image: url(<%= posting.user.photo.url %>);">
                  </div>
                    <div class="card-info1user">
                      <%= posting.user.first_name %>
                    </div>
                    <div class="card-info2user">
                      <%= posting.first_date.strftime("%d %b") + (posting.first_date == posting.last_date ? "" : " ➜ " + posting.last_date.strftime("%d %b"))  %>
                    </div>
                    <div class="card-info3user">
                      <%= posting.theater.name %>
                    </div>
                    <%= link_to posting_pairings_path(posting), method: :post do %>
                      <div class="btnadduser"></div>
                    <% end %>
                </div>
              </div>
            </div>
          <% end %>
        </div>
    </div>

  </div>
</div>
